apiVersion: v1
kind: Service

metadata:
  name: redis-headless

spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: hangeul-chat-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
